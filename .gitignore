# latexmk / pdflatex build artifacts
paper/*.aux
paper/*.log
paper/*.fls
paper/*.fdb_latexmk
paper/*.bbl
paper/*.blg
paper/*.out
paper/*.toc
paper/*.synctex.gz
paper/pic_calculus.pdf
# committed source assets (figures) are NOT build artifacts — keep them tracked
!paper/figures/

# editor / OS
*.swp
.DS_Store
